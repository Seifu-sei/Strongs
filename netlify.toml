[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Ensure SPA fallback works even if not using hash routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200